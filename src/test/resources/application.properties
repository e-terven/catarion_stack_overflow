# Hibernate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.generate_statistics=true
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# MVC properties
spring.mvc.static-path-pattern=/static/**

# Email properties
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.username=noreply.stackover.kata@gmail.com
spring.mail.password=xyrloresdrlwlfid

mail.expiration.time.minutes=15
mail.from.address=noreply.stackover.kata@gmail.com
mail.sender.name="Stackover Kata"
mail.host=http://localhost:8080

# JWT
jwt.secret=${JWT_SECRET:635166546A576E5A7234753778214125442A462D4A614E645267556B58703273}

# Data source
spring.datasource.url=jdbc:${DB_BASE:postgresql}://${DB_SERVER:localhost}:${DB_PORT:5432}/${DB_NAME:test_db}?${DB_PARAMS:characterEncoding=UTF-8&useUnicode=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASS:root}

# Liquibase
spring.liquibase.enabled=false
spring.liquibase.change-log=db/changelog/db.changelog-master.yaml
spring.liquibase.url=jdbc:${DB_BASE:postgresql}://${DB_SERVER:localhost}:${DB_PORT:5432}/${DB_NAME:test_db}
spring.liquibase.user=${DB_USERNAME:postgres}
spring.liquibase.password=${DB_PASS:root}

# Hibernate
spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL}