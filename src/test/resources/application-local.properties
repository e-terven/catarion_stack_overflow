# Data source
spring.datasource.url=jdbc:${DB_BASE}://${DB_SERVER}:${DB_PORT}/${DB_NAME}?${DB_PARAMS:?characterEncoding=UTF-8&useUnicode=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASS}

# Liquibase
spring.liquibase.enabled=false
spring.liquibase.change-log=db/changelog/db.changelog-master.yaml
spring.liquibase.url=jdbc:${DB_BASE}://${DB_SERVER}:${DB_PORT}/${DB_NAME}
spring.liquibase.user=${DB_USERNAME}
spring.liquibase.password=${DB_PASS}

# Hibernate
spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL}

# MVC properties
spring.mvc.static-path-pattern=/static/**

# Email properties
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.username=noreply.stackover.kata@gmail.com
spring.mail.password=xyrloresdrlwlfid

mail.expiration.time.minutes=15
mail.from.address=noreply.stackover.kata@gmail.com
mail.sender.name="Stackover Kata"
mail.host=http://localhost:8080